$defs:
  auth:
    type: object
    properties:
      password:
        type: string
        x-customType: go.opentelemetry.io/collector/config/configopaque.string
      username:
        type: string
    additionalProperties: false
  compression:
    type: object
    properties:
      algorithm:
        type: string
    additionalProperties: false
  replication:
    type: object
    properties:
      class:
        type: string
      replication_factor:
        type: integer
    additionalProperties: false
type: object
properties:
  auth:
    $ref: auth
  compression:
    $ref: compression
  dsn:
    type: string
  keyspace:
    type: string
  logs_table:
    type: string
  port:
    type: integer
  replication:
    $ref: replication
  timeout:
    type: string
    format: duration
  trace_table:
    type: string
additionalProperties: false
