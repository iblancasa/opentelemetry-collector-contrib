$defs:
    database_config:
        additionalProperties: false
        properties:
            host:
                type: string
            password:
                type: string
            port:
                type: integer
            username:
                type: string
        type: object
    network_config:
        additionalProperties: false
        properties:
            host:
                type: string
            port:
                type: integer
        type: object
additionalProperties: false
properties:
    auth:
        x-customType: ./internal.authentication
    database:
        $ref: database_config
    network:
        $ref: network_config
type: object
