$defs:
    other_config:
        additionalProperties: false
        properties:
            baz:
                type: integer
                x-customType: int64
            test:
                $ref: test_config
        type: object
    test_config:
        additionalProperties: false
        properties:
            bar:
                type: integer
            foo:
                type: string
        type: object
